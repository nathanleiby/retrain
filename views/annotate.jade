doctype
html

  body!=body
    #container
      #header
        h1 Retrain
      #main

        hr

        #annotate_messages
          .label
            | Message:
          #message
            p#messageHolder()
              h3 #{message_to_classify.message}
            p#messageClassifications
            each val, key in message_to_classify.classification
              | #{key} : #{val}
              br

        hr

        #category
          .label
            | Category
            p
          .instructions
            | Check if the classification is correct. If so, click "save". If not, update the category, then click "save".
            p
            div
              select
                each category, i in categories
                  option(value=i) #{category}
          .done
            input.nexttask(type='submit', value='Save')
          p
          .floatfix

        #footer
          - var currentYear = new Date().getFullYear();
          | © ImpactLab #{currentYear}

